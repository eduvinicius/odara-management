/**
 * Category creation page (Task 3 placeholder). Fleshed out in Task 12 with
 * the real form wired to the create mutation.
 */
export function CategoryNewPage() {
  return (
    <div className="p-8">
      <h1
        style={{
          fontFamily: 'var(--font-cormorant)',
          color: 'var(--ink-900)',
          fontSize: '1.75rem',
        }}
      >
        Nova Categoria
      </h1>
    </div>
  )
}
