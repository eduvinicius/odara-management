/**
 * Category edit page (Task 3 placeholder). Fleshed out in Task 13 with the
 * real form, pre-filled from the selected category and wired to the update
 * mutation.
 */
export function CategoryEditPage() {
  return (
    <div className="p-8">
      <h1
        style={{
          fontFamily: 'var(--font-cormorant)',
          color: 'var(--ink-900)',
          fontSize: '1.75rem',
        }}
      >
        Editar Categoria
      </h1>
    </div>
  )
}
