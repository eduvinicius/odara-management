/**
 * Category list page (Task 3 placeholder). Fleshed out in Task 8 with the
 * real list, empty state, and error state.
 */
export function CategoryListPage() {
  return (
    <div className="p-8">
      <h1
        style={{
          fontFamily: 'var(--font-cormorant)',
          color: 'var(--ink-900)',
          fontSize: '1.75rem',
        }}
      >
        Categorias
      </h1>
    </div>
  )
}
